name = "qrdurian-mcp"
main = "worker.js"
compatibility_date = "2026-06-01"

# Custom domain — Cloudflare provisions DNS + cert on deploy
routes = [{ pattern = "mcp.qrdurian.com", custom_domain = true }]
